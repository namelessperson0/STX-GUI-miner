{
  "name": "stx-miner",
  "version": "0.2.0",
  "description": "An example of a standalone HTML5 app using GitHub's Electron project (Chrome Engine + Node.js). This shows running a bash shell as a child process and pouring the output into HTML5 elements.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/namelessperson0/STX-GUI-miner.git"
  },
  "keywords": [
    "electron",
    "nodejs",
    "run",
    "shell"
  ],
  "devDependencies": {
    "electron": "^10.1.5"
  },
  "dependencies": {
    "electron-is": "^3.0.0"
  }
}
